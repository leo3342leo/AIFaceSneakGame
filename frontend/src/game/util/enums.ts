export enum KeyCode {
    SPACEBAR=32,
    LEFT=37,
    UP=38,
    RIGHT=39,
    DOWN=40
}

export enum Directions {
    NONE,
    LEFT,
    UP,
    RIGHT,
    DOWN
}

export enum Speed {
    SLOW,
    NORMAL,
    FAST
}

export enum GridElem {
    EMPTY,
    SNAKE,
    FOOD
}